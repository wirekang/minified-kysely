{
  "scripts": {
    "build:clean": "rm -rf build",
    "build": "npm run build:clean && tsc -p tsconfig.build.json",
    "build:test": "npm run build:clean && tsc -p tsconfig.test.json",
    "test": "npm run build:test && jest",
    "test:t": "npm run build:test && jest -t ",
    "start": "npm run build && node build/"
  },
  "dependencies": {
    "@rollup/plugin-terser": "^0.4.4",
    "@rollup/pluginutils": "^5.1.0",
    "rollup": "^4.9.5",
    "rollup-plugin-dts": "^6.1.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "jest": "^29.7.0",
    "prettier": "^3.2.2",
    "typescript": "^5.3.3"
  }
}
